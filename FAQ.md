## Connect to a running docker image with \<usr name\>
```bash
docker exec -it -u <docker_container> bash
```

## Running ohm
```bash
ohmpopcpu <ply_file> <wildcat_traj> [out_file] --ntd=tm ==resolution=X
```
